#sum of n natural number using recurion
def nsum(n):
    if n==0:
        return 0
    return n+nsum(n-1)
num=int(input("enter the number:"))
print("sum of first",num, "natural number is", nsum(num))
